# Makefile

One target, which regenerates `corners.spi`:

```bash
cd ngspice && make corners
```

runs [corners.py](corners.py.md). Run it after editing the `corner:` section of
[cicsim/cicsim.yaml](../cicsim/cicsim.yaml.md), and commit both files.
